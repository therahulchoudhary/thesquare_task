module.exports = {
  env: {},
  images: {
    domains: [],
  },
  // webpack: (config, { isServer }) => {
  //   if (!isServer) {
  //     config.node = { fs: 'empty' };
  //   }

  //   return config;
  // },
};
